{
  "buildCommand": "npm --prefix web install && npm --prefix web run build",
  "outputDirectory": "web/out",
  "framework": null,
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}
